// Auto-generated by LiteXVerify
class {{ agent_name }}_monitor extends uvm_monitor;
  `uvm_component_utils({{ agent_name }}_monitor)

  virtual {{ interface.name }}_if vif;
  uvm_analysis_port #({{ agent_name }}_seq_item) ap;

  function new(string name, uvm_component parent);
    super.new(name, parent);
  endfunction

  function void build_phase(uvm_phase phase);
    super.build_phase(phase);
    ap = new("ap", this);
    if (!uvm_config_db #(virtual {{ interface.name }}_if)::get(this, "", "vif", vif))
      `uvm_fatal("NOVIF", "No virtual interface found for monitor")
  endfunction

  task run_phase(uvm_phase phase);
    {{ agent_name }}_seq_item item;
    forever begin
      @(posedge vif.{{ interface.clock }});
      item = {{ agent_name }}_seq_item::type_id::create("item");
      ap.write(item);
    end
  endtask
endclass
