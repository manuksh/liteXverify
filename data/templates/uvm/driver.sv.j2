// Auto-generated by LiteXVerify
class {{ agent_name }}_driver extends uvm_driver #({{ agent_name }}_seq_item);
  `uvm_component_utils({{ agent_name }}_driver)

  virtual {{ interface.name }}_if vif;

  function new(string name, uvm_component parent);
    super.new(name, parent);
  endfunction

  function void build_phase(uvm_phase phase);
    super.build_phase(phase);
    if (!uvm_config_db #(virtual {{ interface.name }}_if)::get(this, "", "vif", vif))
      `uvm_fatal("NOVIF", "No virtual interface found for driver")
  endfunction

  task run_phase(uvm_phase phase);
    {{ agent_name }}_seq_item req;
    forever begin
      seq_item_port.get_next_item(req);
      drive_item(req);
      seq_item_port.item_done();
    end
  endtask

  task drive_item({{ agent_name }}_seq_item item);
    @(posedge vif.{{ interface.clock }});
    `uvm_info(get_type_name(), $sformatf("Driving: %s", item.convert2string()), UVM_HIGH)
  endtask
endclass
